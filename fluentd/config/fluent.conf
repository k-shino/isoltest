<source>
  type forward
  port 24224
</source>

<match **>
 type copy
 <store>
   type elasticsearch
   host TARGETHOST
   port 9200
   index_name fluentd
   type_name fluentd
 </store>
 <store>
   type file
   path /fluentd/log/log.txt
 </store>
 <store>
  type stdout
 </store>
</match>
